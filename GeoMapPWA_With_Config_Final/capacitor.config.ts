import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.geomappwa',
  appName: 'GeoMapPWA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
